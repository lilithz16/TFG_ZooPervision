<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TFG_LidiaMartinAyuso">

        <!-- LOGIN -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN -->
        <activity android:name=".activities.MainActivity" android:exported="true"/>
        <!-- ANIMALES -->
        <activity android:name=".activities.ListaAnimalesActivity"/>
        <activity android:name=".activities.FormularioAnimalActivity"/>
        <!-- RECINTOS -->
        <activity android:name=".activities.ListaRecintosActivity"/>
        <activity android:name=".activities.FormularioRecintoActivity"/>
        <activity android:name=".activities.ListaAnimalesRecintoActivity"/>
        <!-- INVENTARIO -->
        <!-- <activity android:name=".activities.ListaInventarioActivity"/> -->
        <!-- <activity android:name=".activities.FormularioInventarioActivity"/> -->
        <activity android:name=".activities.StockBajoActivity"/>

        <!-- EMPLEADOS -->
        <!-- <activity android:name=".activities.ListaEmpleadosActivity"/> -->

        <!-- PRODUCTOS -->
        <!-- <activity android:name=".activities.ListaProductosActivity"/> -->

        <!-- VENTAS -->
        <!-- <activity android:name=".activities.ListaVentasActivity"/> -->

        <!-- PROVEEDORES -->
        <!-- <activity android:name=".activities.ListaProveedoresActivity"/> -->

        <!-- PEDIDOS -->
        <!-- <activity android:name=".activities.ListaPedidosActivity"/>  -->
    </application>

</manifest>